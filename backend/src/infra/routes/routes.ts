import { Request, Response, Router } from "express";
// import UserController from "../controllers/UserController";
// import CreateUserUsecase from "../../aplication/CreateUserUsecase";
// import UserRepositoryMemory from "../repository/memory/UserRepositoryMemory";
// import LoginUsecase from "../../aplication/LoginUsecase";

const route = Router();

// const userRepository = new UserRepositoryMemory();
// const createUserUsecase = new CreateUserUsecase(userRepository);
// const loginUsecase = new LoginUsecase(userRepository);
// const userController = new UserController(createUserUsecase, loginUsecase);

// route.post('/user', (req: Request, res: Response) =>  userController.create(req, res));
// route.post('/login', (req: Request, res: Response) =>  userController.login(req, res));

export default route;